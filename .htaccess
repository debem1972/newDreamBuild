<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /DreamBuild/
    
    # Redirecionar requisições para o diretório public
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
